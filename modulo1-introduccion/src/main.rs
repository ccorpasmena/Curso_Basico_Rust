fn main() 
{
    /*  
    - Para crear un proyecto nuevo nos situamos en la terminal en la ruta correcta y escribimos:
    cargo new nombre_del_proyecto

    - Para que se abra Visual Studio Code escribimos: 
    code .

    - En el archivo cargo.toml se muestra información sobre el paquete y las dependencias.

        * El paquete muestra el nombre, la versión de la aplicación y la edición de Rust que usamos.
        * Las dependencias son pequeños programas que usamos para no escribir tanto código porque ya está hecho y funciona.

    - El archivo main.rs contiene la función principal (main).

    - Para arrancar el programa escribimos en la terminal: 
    cargo run
        
    Esto hace que se compile el código que hemos escrito a binario y luego se ejecute.

    - Si solo queremos compilar el código pero NO ejecutarlo escribimos:
    cargo build

    - Para no tener que formatear el código podemos escribir: 
    cargo fmt

    - Para mostrar sugerencias sobre la sintaxis del código podemos escribir: 
    cargo clippy

    */
}
